apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: admin-data
  labels:
    recurring-job.longhorn.io/source: enabled
    recurring-job-group.longhorn.io/s3-backup: enabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
